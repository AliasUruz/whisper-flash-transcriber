# .github/workflows/codex-merge-queue.yml (Versão Final com Gatilho Robusto)

name: 'Codex Merge Queue'

concurrency:
  group: ${{ github.workflow }}-stable
  cancel-in-progress: true

# MELHORIA: Gatilho mais robusto para cobrir mais casos de uso.
on:
  pull_request:
    types: [opened, labeled, reopened, synchronize, ready_for_review]

jobs:
  process_queue:
    name: 'Process PR Queue'
    if: contains(github.event.pull_request.labels.*.name, 'codex')
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write

    steps:
      - name: 'Checkout to stable branch'
        uses: actions/checkout@v4
        with:
          ref: 'stable'
          token: ${{ secrets.CODEX_WORKFLOW_TOKEN }}
          fetch-depth: 0

      - name: 'Configure Git User'
        run: |
          git config user.name "Codex Merge Bot"
          git config user.email "actions-bot@github.com"

      - name: 'Merge the Pull Request'
        env:
          GH_TOKEN: ${{ secrets.CODEX_WORKFLOW_TOKEN }}
          PR_BRANCH: ${{ github.head_ref }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
        run: |
          echo "================================================================="
          echo ">>> Processing PR #${PR_NUMBER} (Branch: ${PR_BRANCH})"
          echo "================================================================="
          
          echo "Attempting to merge ${PR_BRANCH} into stable..."
          if git merge --no-ff "origin/${PR_BRANCH}" -m "Merge pull request #${PR_NUMBER} from ${PR_BRANCH}" -X theirs; then
              echo "✅ Merge successful (with potential automatic conflict resolution)."
              echo "Pushing updated stable branch to origin..."
              git push origin stable
              echo "PR #${PR_NUMBER} merged and pushed successfully."
          else
              echo "❌ CRITICAL: Merge of PR #${PR_NUMBER} failed despite '-X theirs' strategy."
              gh pr comment $PR_NUMBER --body "🚨 **Merge Automático Falhou** 🚨 Ocorreu um conflito de merge crítico que a estratégia automática não conseguiu resolver. Intervenção manual é necessária."
              exit 1
          fi